{{#if success_msg}}
    <div class="alert alert-success">
        {{success_msg}}
        <!--this reference is possible because res.locals.success_msg variable is set -->
    </div>
{{/if}}

{{#if error_msg}}
<div class="alert alert-danger">
    {{error_msg}}
</div>
{{/if}}